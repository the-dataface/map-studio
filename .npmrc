# Configure pnpm to allow postinstall scripts for dependencies
# This is needed for rollup and other build tools
enable-pre-post-scripts=true
# Don't fail on script errors (rollup's patch-package may fail but isn't critical)
scripts-prepend-node-path=true

